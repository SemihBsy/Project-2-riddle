<!DOCTYPE html>
<html lang="en">
    <head>
    <%- include("../partials/head.ejs") %> 
</head>

<body>
    
    <main>
        <div>
            <form action="/riddles/<%= riddle._id %>?_method=PUT" method="post" >
            
                <fieldset>
                    <legend>Edit a riddle</legend>
                    <label>
                        RIDDLE: <input 
                            type="text"
                            name="question"
                            value="<%= riddle.question %>"
                            placeholder="enter a riddle"
                        />
                    </label>
                    <label>
                        ANSWER: <input 
                            type="text"
                            name="answer"
                            value="<%= riddle.answer %>"
                            placeholder="enter a answer"
                        />
                    </label>
                    <label>
                        IMAGE: <input type="text" name="image" <%= riddle.image %> />
                    </label>
                </fieldset>
                <input type="submit" value="Update the Riddle" />
            </form> 

        </div>

    </main>

</body>

</html>